apply plugin: 'org.unbroken-dome.test-sets'

testSets {
    integrationTests { dirName = 'integrationTests' }
}

tasks.integrationTests.dependsOn rootProject.dockerComposeUp

idea {
    module {
        scopes.TEST.minus += [configurations.integrationTestsCompile]
    }
}