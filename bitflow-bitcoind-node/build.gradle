apply plugin: 'com.palantir.docker'

project(':bitflow-bitcoind-node') {

    docker {
        name 'docker.io/daedalus2718/bitcoind'
        dockerfile 'Dockerfile'
        files 'bitcoind.logrotate'
    }
}
